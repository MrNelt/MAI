Основные средства Командного Языка операционной системы UNIX.
---------------------------
a=b  - присваивание
$  - взять значение
echo  оператор вывода
;  - разделитель команд
'...' - как есть
"..."  - возможно изменение (вставка значений)
\   - отменить спец значение след символа
{ } - область действия спец символа
set  - вывод всех переменных и др.
$1 $2   ${10}  - спец переменные (сохр параметры)
  read  - оператор ввода,чтение
let   - арифметические выражения
test  - проверка условия
&&  ||  условные фильтры 
$#  - число параметров
$?  - признак завершения предыдущей команды
$@  - список параметров
chmod a+x  - даёт статус выполняемого файла
shift  - сдвиг параметров
#  - комментарий
#!  - место нахождения интерпретатора
if ... then ...[else ...] fi 
[ ... ] - условие
  
`...`  - результат выполнения команды 
while ...;do ...;done  цикл
expr  - вычисление выражения  ???
eval  -выполнения команды

for i; do ...; done # for i in $@;
for i in 
$[ ... ]  - вычисление выражения 
[[ ... ]] - условия как в СИ
----------------------------------
exit  - выход из скрипта
echo -n - вывод без перехода на следующую строку
break - выход из цикла
continue  - переход к началу цикла
function - описание функции
$(( ... ))  - результат вычисление выражения как в Си
$( ... )  -  результат выполнения функции (команды, утилиты).
${#aaa} длина содеожимого переменной aaa (длина строки).
${aaa:3:4}  - часть строки ааа (интервал) ${строка:от:сколько}.
${aaa:0:4}  - начало строки ааа из 4-х символов
${aаа:$[ ${#aaa} - 4 ]}  - хвост строки ааа из 4-х символов
${@:2:3} - часть списка аргументов
${@:$[ $# - 3 ]}  - последние 4 списка аргументов
arr=(aaaa bbb ccc dd e) - создание массива arr
${arr}  - содержимое 1-го элемента массива arr
${arr[@]}  - содержимое всего массива arr
${arr[2]}  - содержимое 3-го элемента массива arr
${#arr} - длина 1-го элемента массива arr
${#arr[@]} - число элементов массива arr
for((...));do ...cat ;done  - цикл for как в Си.